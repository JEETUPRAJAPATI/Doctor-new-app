import { StyleSheet } from 'react-native';

export const globalStyles = StyleSheet.create({
  scrollView: {
    flex: 1,
  },
  scrollContent: {
    flexGrow: 1,
  },
  noScrollbar: {
    scrollbarWidth: 'none',
  },
});